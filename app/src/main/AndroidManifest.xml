<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.virtblch.calcdatesas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="SAS Date Calculator"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CalcDateSAS">
        <activity
            android:name="com.virtblch.calcdatesas.TimeToSAS"
            android:exported="false" />
        <activity
            android:name="com.virtblch.calcdatesas.SAStoTime"
            android:exported="false" />
        <activity
            android:name="com.virtblch.calcdatesas.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>